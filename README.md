Simple pomodoro timer all made using front-end script. Animations are using Sal.js.
The timer is working but there are restrictions such as: The user must add at least one subject and choose it before the session ends,if not it will give an error because the targetSubject variable will be not defined.
